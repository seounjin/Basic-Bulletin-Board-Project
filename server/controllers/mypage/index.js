const { getProfile } = require('./getProfile');
const { checkPassword } = require('./checkPassword');
const { changePrivacy } = require('./changePrivacy');
const { setAvatar } = require('./setAvatar');


module.exports = {
    getProfile, checkPassword, changePrivacy, setAvatar
};